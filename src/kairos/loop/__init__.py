"""Kairos nightly loop package.

Modules in this package:
  db.py      — Postgres connection helper + migration runner (Day 10)
  persist.py — Ingest findings/rollup rows (Day 10, wired after Day-8 detectors)
  delta.py   — Delta engine for trend computation (Day 11)
  discover.py — Discovery mode / flywheel engine (Day 12)
"""
